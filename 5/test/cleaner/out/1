%This is a dirty latex code with \qadd{something to add \qdel{or delete} and some comments}.
This is \cadd{another} line of dirty latex code with something to add but with \{escaped braces\} and some percentages \% with some  commands \textbf{and} more comments %such \qadd{as this}.
